"use client";

/**
 * Portfolio sun image — idle while LightSkyBackground renders the Framer sun.
 * Re-enable if we want the PNG disc back alongside / instead of the CSS sun.
 */
export default function HeroSun() {
  return null;
}
